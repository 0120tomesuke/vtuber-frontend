{
  "name": "hololive-live-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create hololive-live-monitor",
    "db:migrate": "wrangler d1 migrations apply hololive-live-monitor --remote",
    "test": "node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "googleapis": "^171.4.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
